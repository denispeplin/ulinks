<%= form_for(@forum_link) do |f| %>
  <% if @forum_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum_link.errors.count, "error") %> prohibited this forum_link from being saved:</h2>

      <ul>
      <% @forum_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :path %><br />
    <%= f.text_field :path %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
